root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/api ./cmd/api"
  bin = "./tmp/api"
  include_ext = ["go", "sql"]
  exclude_dir = ["tmp", "bin", ".git", ".claude"]
  exclude_regex = ["_test.go"]
  delay = 500

[log]
  time = false
